{
  "name": "@aus-dash/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aus-dash/db": "workspace:*",
    "@aus-dash/shared": "workspace:*",
    "@hono/node-server": "^1.13.7",
    "@hono/standard-validator": "^0.2.2",
    "@valibot/to-json-schema": "^1.5.0",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.6.12",
    "hono-openapi": "^1.2.0",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.9",
    "vitest": "2.1.9"
  }
}
